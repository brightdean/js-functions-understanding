function myFunction(num, num1) {
    console.log(num1)
}

let num = 10
let num1 = 2

myFunction(num)

//expected undefined because we pass num as the first argument and we leave the second arg empty, so it prints out undefined

//got undefined